import { button } from './button';
import { customUrl } from './custom-url';
import { pageBuilder } from './pagebuilder';
import { portableText, portableTextHeading } from './portable-text';

export const definitions = [
  customUrl,
  portableText,
  button,
  pageBuilder,
  portableTextHeading,
];
